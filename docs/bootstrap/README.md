# Bootstrap Context

This folder contains initial context to help AI agents understand:

- the Django backend (source of truth)
- the expected scope of the AI backend

These files are not final documentation and will evolve or be removed over time.